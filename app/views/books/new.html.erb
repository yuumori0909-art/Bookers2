<h1>New book</h1>
<%= form_with model: @book do |book| %>
  <h4>Title</h4>
  <%= book.text_field :title %>
	
  <h4>Opinion</h4>
  <%= book.text_area :body %>
	
  <%= book.submit 'Create Book' %>
  
<% end %>
